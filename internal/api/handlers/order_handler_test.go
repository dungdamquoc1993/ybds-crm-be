package handlers_test

import (
	"testing"
)

func TestOrderHandler(t *testing.T) {
	// Skip tests for now until we can properly mock the services
	t.Skip("Skipping order handler tests until we can properly mock the services")
}
