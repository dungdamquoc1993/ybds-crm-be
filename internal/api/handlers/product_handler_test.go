package handlers_test

import (
	"testing"
)

func TestProductHandler(t *testing.T) {
	// Skip tests for now until we can properly mock the services
	t.Skip("Skipping product handler tests until we can properly mock the services")
}
